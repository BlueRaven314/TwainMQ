{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"TwainMQ","text":"<p>TwainMQ is a lightweight, file\u2011based message log inspired by Kafka\u2019s design principles but without the operational overhead of running a broker or cluster. It provides durable, append\u2011only topics, partitioned message streams, consumer groups with load balancing, and offset managemen, all without using a central service or broker.</p> <p>TwainMQ is designed for:</p> <ul> <li>local development workflows  </li> <li>embedded systems  </li> <li>single\u2011machine (or small local network) data pipelines  </li> <li>teaching and experimentation with log\u2011based messaging  </li> <li>applications that need Kafka\u2011like semantics without a server  </li> </ul> <p>It requires no services, no central authority, just a directory where the files will live.  All coordination is done via the log files.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>No running services or brokers, just an API and distributed self-organisation</li> <li>File\u2011based topics stored as append\u2011only logs</li> <li>Partitions for parallelism and ordering guarantees</li> <li>Consumer groups with automatic load balancing</li> <li>Offset tracking and commits</li> <li>Typed messages (supports dataclasses)</li> <li>Simple API for producers and consumers</li> </ul>"},{"location":"#important-differences-from-other-message-brokers","title":"Important differences from other message brokers","text":"<p>If you are used to other message queues (especially Kafka), then much will seem familiar but there are a few key places where things are different that are worth knowing about up front</p> <ul> <li>Consumers always round robin their paritions.  In kafka if you are behind you might get given a big block of messages from one partition while making no progress on another.  TwainMQ always rotates around the partitions providing the next from each queue in turn if they have any.</li> <li>Rebalances are cheap (often free).  Rebalances can be called by anyway, and should be called fairly frequently to check all you consumers are still alive.  If there is nothing wrong a rebal is a no-op.  Even when a rebalance does more partitions, priority is given to retaining your existing partitions.  Rebalances do not pause message consumption.</li> <li>Messages must not exceed 4kb.  Future work will remove this restriction, but for now each message must not exceed 4kb onces compressed and encoded.</li> <li>Keys must be integers.  Future work will remove this restriction by implementing a key lookup table (so the true keys will still be integers), however for now you can only use integer keys.</li> </ul>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>from twainmq import TwainMQ\n\ntmq = TwainMQ(\"/tmp/twain\")\n\n# Create a topic\ntmq.create_topic(\"events\")\n\n# Write a message\nproducer = tmq.producer(\"events\")\nproducer.write_message(42, \"hello world\")\n\n# Read messages\nconsumer = tmq.consumer(\"events\")\nwhile message := consumer.poll():\n    print(message)\n</code></pre>"},{"location":"#documentation","title":"Documentation","text":"<p>Use the navigation sidebar to explore:</p> <ul> <li>Getting Started \u2014 installation and your first pipeline  </li> <li>Tutorials \u2014 practical examples and patterns  </li> <li>Architecture \u2014 how TwainMQ works internally  </li> <li>API Reference \u2014 auto\u2011generated from docstrings  </li> </ul>"},{"location":"#source-code","title":"Source Code","text":"<p>TwainMQ is open source and available on GitHub:</p> <p>https://github.com/BlueRaven314/TwainMQ</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide walks you through installing TwainMQ, creating your first topic, producing messages, and consuming them with a consumer group.</p>"},{"location":"getting-started/#installation","title":"Installation","text":"<p>TwainMQ is not yet published on PyPI, so install it directly from the repository:</p> <pre><code>pip install git+https://github.com/BlueRaven314/TwainMQ.git\n</code></pre> <p>Or clone the repo for development:</p> <pre><code>git clone https://github.com/BlueRaven314/TwainMQ.git\ncd TwainMQ\npip install -e .\n</code></pre>"},{"location":"getting-started/#creating-a-message-store","title":"Creating a Message Store","text":"<p>TwainMQ stores all data in a directory you choose:</p> <pre><code>from twainmq import TwainMQ\n\nmq = TwainMQ(\"/tmp/twain\")\n</code></pre> <p>This directory will contain:</p> <ul> <li>topic folders  </li> <li>partition files  </li> <li>offset tracking  </li> <li>consumer group metadata  </li> </ul> <p>Everything is plain files \u2014 easy to inspect and debug.</p>"},{"location":"getting-started/#create-a-topic","title":"Create a topic","text":"<p>A topic must be created before you can publish to it</p> <pre><code>from twainmq import TwainMQ\n\ntmq = TwainMQ(\"/tmp/twain\")\n\ntmq.create_topic(\"events\", key_type = \"u16\", partitions = 4)\n</code></pre> <p>This will create a topic called \"events\" where messages are split between 4 partitions.  All messages must be published with a key, and this topic will use <code>u16</code> type keys (which is the default anyway).  We have not specified any message types, so this topic will be for sending strings (byte strings or utf-8).</p>"},{"location":"getting-started/#producing-messages","title":"Producing Messages","text":"<p>Create a producer for a topic and send messages:</p> <pre><code>producer = tmq.producer(\"events\")\n\nproducer.send(12, \"Hello world\")\n\nx = 12345\nproducer.send(42, x.to_bytes(16))\n</code></pre> <p>Messages are appended to the topic\u2019s log file.</p>"},{"location":"getting-started/#consuming-messages","title":"Consuming Messages","text":"<p>Consumers read from topics and track their own offsets:</p> <pre><code>consumer = mq.consumer(\"events\", group=\"event-readers\")\n\nprint(consumer.poll())\nconsumer.commit()\nprint(consumer.poll())\n\n</code></pre>"},{"location":"getting-started/#what-happens-here","title":"What happens here?","text":"<ul> <li>The consumer joins the event-readers consumer group, creating it if it doesn't already exist</li> <li>The consumer triggers a rebalance event for the group, and offers to accept some partitions</li> <li>At the end of the rebalance period (default 60 seconds) partitions are confirmed with each consumer (in our case only 1 consumer, so it will get all of them)</li> <li>Polling returns the next message for that consumer (round robin across the partitions it owns)</li> <li></li> </ul>"},{"location":"getting-started/#consumer-groups","title":"Consumer Groups","text":"<p>Multiple consumers in the same group share the work:</p> <pre><code>c1 = mq.consumer(\"events\", group=\"workers\")\nc2 = mq.consumer(\"events\", group=\"workers\")\n</code></pre> <p>TwainMQ will:</p> <ul> <li>detect both consumers  </li> <li>rebalance partitions  </li> <li>ensure each partition is processed by exactly one consumer  </li> </ul> <p>This mirrors Kafka\u2019s consumer group semantics \u2014 but locally.</p>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<p>Check out:</p> <ul> <li>Tutorials for real\u2011world examples  </li> <li>Architecture to understand how TwainMQ works internally  </li> <li>API Reference for details on every class and method  </li> </ul> <p>TwainMQ is intentionally small and readable \u2014 feel free to explore the source code and adapt it to your needs.</p>"},{"location":"api/reference/","title":"API Reference","text":"<p>TwainMQ is a broker free messaging system.  Unlike similar tools like Apache Kafka and RabbitMQ, TwainMQ does not have any running services or brokers to coordinate messages, being run entirely from files.  Think of it like SQLite is to traditional database systems.</p> <p>To set up a TwainMQ message queue you simply need to specify a directory for it to use, either locally or on a network share.</p> <p>Messages are limited in size to ensure that they can be written quickly.  The size limit is currently set at slightly less than 4kb.</p> <p>Messages can be either plain UTF-8 or raw bytes.</p> <p>Messages are organised by key, partition and topic.</p>"},{"location":"api/reference/#twainmq--keys","title":"Keys","text":"<p>u8 u16 u32 u64 str</p>"},{"location":"architecture/","title":"Architecture","text":"<p>This section will describe the internal design of TwainMQ, including:</p> <ul> <li>Storage format and file layout</li> <li>Partitioning model</li> <li>Consumer group coordination</li> <li>Offset tracking and commits</li> <li>Message lifecycle</li> </ul> <p>Content will be added as the project evolves.</p>"},{"location":"architecture/message_keys/","title":"Keys","text":"<p>Every message must have a message key when it is published.  The key must be a specific type set when the topic is created.  Currently keys can be either unsigned integer or string, but they must have a fixed byte width.  The default for a topic if not specified is \"u16\".</p> <p>Currently allowed types are u8, u16, u32, u64, char1, char2, char4, char8 and char16.</p>"},{"location":"architecture/message_keys/#parition-allocation","title":"Parition allocation","text":"<p>Messages are scattered across partitions based on their key, so that two messages with the same key always end up on the same partition.  In order to attempt to balance the distribution, keys are hashed using SplitMix64 and the result used to select the topic.</p>"},{"location":"tutorials/","title":"Tutorials","text":"<p>This section will contain practical, step\u2011by\u2011step guides for using TwainMQ.</p> <p>Planned topics include:</p> <ul> <li>Building a simple message pipeline</li> <li>Using consumer groups</li> <li>Working with partitions</li> <li>Debugging message flow</li> </ul>"}]}